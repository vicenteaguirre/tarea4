#include "nodo.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

//Balance
int getBalance(Nodo * N);

// Right rotate
Nodo *rightRotate(Nodo * y);

// Left rotate
Nodo *leftRotate(Nodo *x);